# 한국수출입은행 인증키 (기존 .env의 api_key 사용)
api_key=
# Supabase > Project Settings > Data API > Project URL
SUPABASE_URL=
# Supabase > Project Settings > API Keys > Secret key (서버 전용)
SUPABASE_SECRET_KEY=
# 32바이트 이상의 무작위 문자열. Vercel Cron 및 수동 수집 인증에 사용
CRON_SECRET=
# 로컬에서만 선택 사항
PORT=3000
